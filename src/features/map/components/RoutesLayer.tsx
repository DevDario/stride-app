import type { RouteRecord } from '../types/map.types';

interface RoutesLayerProps {
  routes: RouteRecord[];
}

// TODO: implement full route visualisation layer — render route polylines
// on the map using RouteLine component once real route data is available
export function RoutesLayer(_props: RoutesLayerProps) {
  return null;
}
