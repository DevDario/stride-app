import type { RecordEntry } from '../types/map.types';

interface RecordsLayerProps {
  records: RecordEntry[];
}

// TODO: implement records layer — render podiums / trophies as map symbols
// once real records data is available from the backend
export function RecordsLayer(_props: RecordsLayerProps) {
  return null;
}
